function setup() {
  createCanvas(outerWidth, outerHeight*2);
}

function draw() {
  background(1);
  fill('lightBlue')

  textSize(80)
  textAlign(CENTER)
  text("Hello, and welcome to CutePockets!", width/2, 80)
  textSize(50)
  text('Here, we will')

}
